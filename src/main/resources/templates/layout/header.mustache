<!--페이지 로딩 속도를 높이기 위해 css 는 header 에 js 는 footer 에 두었다
 html 은 위에서부터 실행되기 때문에 head 가 다 실행되고서야 body 가 실행된다.
 head 가 다 불러지지 않으면 사용자 쪽에선 백지만 노출된다. js의 용량이 커질 수록 body 부분의 실행이 늦어지기 때문에
 js 는 body 하단에 두어 화면이 다 그려진 뒤에 호출하는 것이 좋다.-->

<!DOCTYPE HTML>
<html>
<head>
    <title>스프링부트 웹서비스</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <!--css 는 화면을 그리는 역할이므로 head 에서 불러온다. -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>